pipeline {
    agent any 
    stages {
    stage ('pull'){
        steps {
            git credentialsId: 'github', url: 'https://github.com/oselukar/vue-starter.git'
            
        } 
                   }
 
 stage ('Docker-Build') {
     steps {
         sh 'docker build -t omkesh/development:v${BUILD_NUMBER}-${GIT-BRANCH} .'
         
     }
 }
stage ('docker push'){
     steps { 
         withDockerRegistry([credentialsId: 'dockerhub' , url: ""]){
             sh 'docker push omkesh/development:v${BUILD_NUMBER}-${GIT-BRANCH}'
         }
     }
    }
}
}
